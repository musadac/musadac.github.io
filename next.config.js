/** @type {import('next').NextConfig} */
const nextConfig = {
output: 'export',
}

module.exports = {
  reactStrictMode: true,

 // Add basePath
  // basePath: '/github-pages',
  // output: 'export',
}
